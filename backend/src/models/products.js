const dbpool = require('../config/database');


// const getAllProducts = () => {

// }


const creareNewProduct = () => {
    const querry = `INSERT INTO products(name, description, quantity)
                    VALUES('')`
} 